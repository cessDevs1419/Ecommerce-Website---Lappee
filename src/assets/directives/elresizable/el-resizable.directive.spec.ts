import { ElResizableDirective } from './el-resizable.directive';

describe('ElResizableDirective', () => {
  it('should create an instance', () => {
    const directive = new ElResizableDirective();
    expect(directive).toBeTruthy();
  });
});
